# ROCm (AMD GPU) requirements for gaze-estimation
# Tested with ROCm 6.x on RDNA2/RDNA3 hardware.
#
# IMPORTANT: Do NOT install both this file and requirements.txt together.
# They conflict on torch / onnxruntime.
#
# Install with:
#   pip install -r requirements-rocm.txt
#
# Or use the Makefile target:
#   make install-rocm

# PyTorch with ROCm backend (HIP)
# Pick the correct rocm version from https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/rocm6.2
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# ONNX Runtime with ROCm execution provider
# Note: onnxruntime-rocm is a separate wheel from onnxruntime
onnxruntime-rocm>=1.17.0
onnx>=1.14.0

# Core vision (same as CPU)
opencv-python>=4.8.0
numpy>=1.24.0
mediapipe>=0.10.0

# Utilities
pyyaml>=6.0
pydantic>=2.0.0
scipy>=1.10.0
pygame>=2.5.0
screeninfo>=0.8.1
